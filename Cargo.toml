[package]
name = "norx_kernel"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-only"

[dependencies]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
codegen-units = 1
